function SingleMessage({error}: { error: string }) {    return <span className="block mb-2.5 text-red-700 text-xs">{error}</span>}function MultipleMessages({errors}: { errors: [] }) {    return <ul>        {errors.map((e, i) => <li key={i}><SingleMessage error={e}/></li>)}    </ul>}export default function ErrorMessages({errors}: { errors?: string | [] }) {    if (!errors) {        return '';    }    return Array.isArray(errors) ? <MultipleMessages errors={errors}/> : <SingleMessage error={errors}/>}